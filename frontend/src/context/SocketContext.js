// ===========================================
// CONTEXT: SocketContext
// RÔLE: Définition du contexte uniquement
// ===========================================

import { createContext } from 'react';

export const SocketContext = createContext(null);